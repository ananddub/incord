FROM golang:1.26-alpine

RUN apk add --no-cache git gcc musl-dev

WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN go build -buildvcs=false -o /app/init ./cmd/init

# Run from /app so migration file paths resolve correctly
WORKDIR /app
ENTRYPOINT ["/app/init","&& rm","init"]
