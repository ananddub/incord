FROM golang:1.26-alpine

RUN apk add --no-cache git gcc musl-dev

RUN go install github.com/air-verse/air@v1.61.7

WORKDIR /app

# No COPY - source code comes from volume mount at runtime
# Air handles build + hot-reload on file changes

ENTRYPOINT ["air"]
