global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "ndiscord-server"
    static_configs:
      - targets: ["server:9100"]
        labels:
          service: "ndiscord"
